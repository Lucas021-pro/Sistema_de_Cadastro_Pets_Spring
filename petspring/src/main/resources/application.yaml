spring:
  application:
    name: petspring
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/abrigo?createDatabaseIfNotExist=true
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
